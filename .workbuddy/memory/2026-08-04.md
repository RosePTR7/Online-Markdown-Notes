## 2026-08-04
- 通读整个 Online Notes (Test) 项目源码（Vue3+Vite Markdown 笔记应用），理清架构并写入 `MEMORY.md`：在线模式(IndexedDB/Dexie)+本地模式(File System Access API)双模式，4 个单例 store、Vditor 编辑器封装、防抖自动保存+撤销栈、导入导出、AI 润色。
- 处理「SVG 图标重复」冗余（原清单第2条）：新建 `src/components/Icon.vue` 统一登记全部 9 个图标（rename/add-folder/add-note/delete/move-unsorted/refresh/doc/folder/spinner），并替换 FolderTree、LocalFolderTree、FolderItem、LocalFolderItem、UnsortedNotes、Sidebar、EditorArea 共 7 个组件里所有内联 SVG（约 25+ 处）。构建通过（✓ built，92 modules）。新增图标一律走 Icon.vue，禁止再内联 path。
- 处理「弹窗系统重复 - 手搓悬浮窗」：将 `EditorArea.vue` 中手搓的 AI 配置弹窗（原 `fixed inset-0 bg-black/50` 居中带遮罩窗口）改造为复用通用 `FloatingPanel` 组件（`:visible="showSetting" title="AI接口配置" panel-class="w-96" @close="showSetting=false"`），与查找替换/AI润色/消息/输入悬浮窗统一风格。注意：改造后不再是模态遮罩，变为可拖拽非模态浮动窗（与同伴一致）。构建通过。剩余手搓弹窗（不在本次范围）：Sidebar 的删除确认+选择文件夹两个模态框应改走 `Modal.vue`/`useModalStore.confirm/prompt`；各右键菜单/下拉菜单应统一抽 `ContextMenu` 组件。
- 处理「Sidebar 手搓模态框」冗余：删除 Sidebar.vue 中两个手搓模态框（删除确认 + 选择文件夹 prompt），改为调用单例 `useModalStore`：`handleCreateNote`/`handleCreateFolder` 在无文件夹分支用 `modalStore.confirm({title,message,confirmText})` 拦回引导选文件夹；`selectLocalFolder` 移除 `showSelectFolderPrompt` 残留状态。删除死代码：`showSelectFolderPrompt`、`showDeleteModal`/`deleteMessage`/`pendingDeleteId`/`pendingDeleteType`、`openDeleteConfirm`/`confirmDelete`/`defineExpose({openDeleteConfirm})`（经 grep 确认 `openDeleteConfirm` 外部零调用）。构建通过。
- 处理「右键/下拉菜单重复」冗余：新建 `src/components/ContextMenu.vue`（Teleport to body + `fixed inset-0 z-[100]` 遮罩层 `@click/@contextmenu.prevent="close"` + 内层定位菜单 div 无 `@click.stop` 以便条目点击冒泡到遮罩自动关闭，需保持打开的条目用 `@click.stop`；props: `visible`/`x`/`y`/`menuClass`，`inject('isDark')` 暗色适配）。将 FolderTree、LocalFolderTree、UnsortedNotes 三处右键菜单及 EditorArea 的 标签右键菜单、顶栏 文件/编辑/查看 下拉、导出二级子菜单 全部改为复用 ContextMenu。EditorArea 脚本：`togglePanel(panel, btnRef)` 现按触发按钮 `getBoundingClientRect()` 计算 `dropdownX`/`dropdownY`；删除废弃的 `closePanelOnClickOutside` 全局 click 监听及其 `onBeforeUnmount` 清理（由 ContextMenu 遮罩接管外部点击关闭）；从 vue 导入移除 `onBeforeUnmount`。构建通过（`.build-verify` 临时目录，已清理）。至此原 6 点冗余清单第 2/4 条彻底完成，菜单/弹窗已统一到 Icon/FloatingPanel/Modal/ContextMenu 四套通用件。
- **处理「本地/在线侧边栏外观重复」（最重要任务）**：合并两套独立编写但外观应完全相同的侧边栏树。做法——(1) 重写 `FolderItem.vue` 为**单一样板**，在线/本地共用同一 DOM 与样式，仅数据/逻辑分叉：在线走 `folderStore`（`getChildFolders`/`isFolderExpanded`/`moveFolder`/拖拽），本地走 `props.contents`（懒加载）并通过 `emit('expand')`+`emit('open-note')` 上抛；用 `props.isLocal` 控制拖拽 `:draggable` 与 handler 早返回，用 `inject('localNotesMap')` 解析本地文件名→标题/激活态。(2) 重写 `FolderTree.vue` 为**单一入口**，内部按 `isLocal = localModeStore.mode.value === 'local'` 分叉：在线用 `getTopLevelFolders`，本地内嵌原 LocalFolderTree 的扫描/`folderCache`/`refreshScan`/`resetAndScan`/`mergeItems` 逻辑（新增 `findNodeByPath`/`setNodeContents` 按路径就地写入节点 contents，修复原嵌套懒加载只按 name 查找的缺陷）；在线与本地共用一套 `<ContextMenu>`，菜单项外观一致、逻辑按 `menuType`('folder'|'note'|'file') 与 `isLocal` 分派（重命名/新建子文件夹/新建笔记/删除 文件夹，在线额外有 笔记的 重命名/移至未分类/删除，本地文件仅 删除）。`provide('openContextMenu')` 同时供在线与本地 FolderItem 打开菜单。(3) `Sidebar.vue` 内容区改为两种模式都渲染同一个 `<FolderTree ref="folderTreeRef" @open-note="handleOpenLocalNote">`，在线模式在其后追加 `<UnsortedNotes/>`；`localFolderTreeRef`→`folderTreeRef`；删除死代码 `onLocalRefresh`。(4) 删除 `LocalFolderTree.vue` 与 `LocalFolderItem.vue` 两个冗余文件。构建通过（`.build-verify`，已清理）。核心原则：外观/样板只写一份，模式差异只在数据来源与少数事件处理上。
- **修复两个代码 BUG（用户指定只修 1、2，保留 3）**：(1) `src/utils/importExport.js` 的 `escapeHtml` 映射值原本是字面量字符（`&→&`、`<→<` 等），只有单引号被转义，导致导出 HTML 时标题含 `<>&"` 会生成非法 HTML；改为正确的 `&amp; &lt; &gt; &quot; &#39;`。(2) `src/stores/useLocalModeStore.js` 的 `addLocalNote`/`addLocalFolder` 先 `mergeIntoCache(...)` 把新项并入缓存、紧接着 `invalidateCache(...)` 又把同一缓存键删除——前一行自相矛盾且无效；删除两处 `mergeIntoCache` 调用，保留 `invalidateCache`（确保下次扫盘读到新文件），并删除因此变为死代码的 `mergeIntoCache` 函数定义（grep 确认零引用）。**未按原清单修 BUG 3**：`FolderItem.vue` 第 35 行 `:class="isDark ? 'text-amber-400' : 'text-amber-400'"` 两个分支完全相同（死亡三元），用户要求保留，为以后可能的外观改变留机会。**构建通过**（`.build-verify` 临时目录，已清理）。
- **解决「写法冗余」第 4（暗色三元菜单项样板）→ 抽 `MenuItem.vue` 组件 + 第 5（`stripMd`/本地标题解析重复）→ 抽 `localNotes.js` 共享 helper**：(1) 新建 `src/components/MenuItem.vue`：统一 ContextMenu 菜单项外观——props `icon`/`label`/`danger`/`active`/`stopPropagation`/`padding`，内部 `inject('isDark')` 切换 hover/active 暗色样式，基础类含 `relative` 以支持子菜单箭头绝对定位；`defineExpose({rootEl, getBoundingClientRect})` 供父组件（导出子菜单定位）取根元素矩形；`stopPropagation` 时 onClick 调 `e.stopPropagation()` 阻止冒泡到 ContextMenu 遮罩以免菜单误关。(2) 新建 `src/utils/localNotes.js`：导出 `stripMd`/`resolveLocalTitle(map,filename)`/`isLocalActive(map,filename,activeId)`，集中 FolderTree 与 FolderItem 原先各写一遍的 `stripMd` + `localNotesMap` 查找标题/激活态逻辑。(3) 将 `FolderTree.vue`、`FolderItem.vue`、`EditorArea.vue`（顶栏 文件/编辑/查看 下拉、导出二级子菜单、标签右键菜单，其中导出触发项保留 `ref="exportMenuItemRef"`+`:active="showSubMenu==='export'"`+`stop-propagation`、视图项用 `:active="themeMode===..."`）、`UnsortedNotes.vue` 四处菜单的全部重复菜单项 div 改为 `<MenuItem>`，并删除对应组件里原先的 `stripMd` 本地定义（FolderItem/FolderTree 改用共享 helper）。**构建通过**（`.build-verify` 临时目录，已清理）。注意：分隔线 div 因非"菜单项"冗余样板保留内联；导出子菜单定位依赖 `exportMenuItemRef.value.getBoundingClientRect()`，MenuItem 已暴露该方法，EditorArea 脚本无需改。
- **统一"新建笔记"入口（按用户确认的方案落地）**：(1) `useNoteStore.js`：`addNote(folderId, title, content)` 增强为可接收可选标题/内容（缺省回退原默认，修复在线模式新建时用户输入的名字被丢弃的不一致）；新增 `createNote(folderId, title, content)` 门面（按 `useLocalModeStore().mode` 分派：本地 → `createLocalNote`、在线 → `addNote`），组件不再各自写 `if(isLocal)`；新增 `importNote(note)`（保留原始 id，供 JSON 备份恢复，避免破坏文件夹外键）；在 `createLocalNote` 写盘失败 catch 内加 `useModalStore().showToast('新建笔记失败...')` 集中失败提示（此前仅 FolderTree 局部 toast，Sidebar 静默失败）；导出 `createNote`/`importNote`。(2) `Sidebar.vue`：`handleCreateNote` 删除 `if(isLocal){...}else{addNote(null)}`，改为先本地无目录引导选择、再统一 `createNote(null, name, '')`，本地成功则 `folderTreeRef.refreshScan`。(3) `FolderTree.vue`：`addNoteInFolder` 删除本地 try/catch+toast 与在线 `addNote` 分支，统一 `createNote(dirPath, name, '')`，仅本地再按 dirPath 刷新目录。(4) `EditorArea.vue`：`addNoteFromImport` 由手搓 `noteTable.add+push+openNote` 改为复用 `addNote`；`handleJSONImport` 循环由手搓改为复用 `importNote`（保留 id）；移除已无用的 `nanoid` 导入。至此 UI 触发点（Sidebar 按钮、FolderTree 右键菜单）与导入路径全部只写外观/触发，功能统一走 store 的 `createNote`/`addNote`/`importNote`。构建验证 `npm run build -- --outDir .build-verify-fresh3` → `✓ built in 970ms` EXIT=0（仅 gray-matter eval/chunk 无关警告）。已把 `.build-verify-fresh3` 加入 `.gitignore`。
- **推进优化清单 #9（拖拽 handler 参数化）+ #10（菜单 handler 本地分支样板收口）**：#9 在 `FolderItem.vue` 把 6 个拖拽 handler 合并为 3 个参数化版本——`handleDragStart(type,id,e)`（合并 folder/note 启动）、`handleDragOver(isNote,e)`（文件夹三区 before/after/inside、笔记两区 before/after，按比例阈值区分）、`handleDragLeave(e)`（原 handleDragLeave/handleNoteDragLeave 完全相同直接合一）；新增 `resetDragVisual()`（复位 isDragOver/dragPosition，供 dragEnd/leave/drop 共用）与 `finishDrop()`（clearDragState+resetDragVisual，供 handleDragEnd 与两处 drop 收尾）；模板拖拽绑定改为内联箭头 `(e) => handleDragStart('folder'|'note', id, e)` / `handleDragOver(false|true, e)` / `handleDragLeave(e)`。#10 在 `FolderTree.vue` 抽 `findFolderById(id)`（替代 `folderList.value.find(f=>f.id===target)` 重复查找）与 `runLocalOp(fn, msg, {refresh=true})`（统一包裹本地写盘的 try/catch + console.error + showToast + refreshScan）；`startRename`/`addSubFolder`/`confirmDeleteFolder` 的本地分支与 `confirmDeleteLocalNote` 改用这两个 helper，删掉各自重复的 try/catch/toast/refreshScan 样板。说明：文件夹菜单在线/本地共用 `menuType==='folder'`，模式判定这一处分支不可消除；未做"策略表"是因为会把每个动作拆成两份函数、反而降低可读性，故只收口真正重复的本地操作样板。构建验证 `npm run build -- --outDir .build-verify-fresh2` → `✓ built in 1.20s` EXIT=0（仅 gray-matter eval/chunk 体积无关警告）。已把 `.build-verify-fresh2` 加入 `.gitignore`。至此原优化清单的逻辑/写法/量类项目基本清零，剩余可考虑项仅"拖拽落点逻辑 handleDrop/handleNoteDrop 是否进一步合并"（二者落点语义差异大，维持现状更清晰）。
- **核验「MenuItem / localNotes / EditorArea 改用 MenuItem / 构建验证」五项任务均已完成**：(1) `MenuItem.vue` 存在且实现完整（props icon/label/danger/active/stopPropagation/padding + inject isDark + expose getBoundingClientRect）；(2) `FolderTree.vue` 右键菜单 9 处菜单项（重命名/新建子文件夹/新建笔记/删除文件夹/笔记重命名/移至未分类/删除笔记/删除本地笔记）已全部改用 `<MenuItem>`，并 import 自 `./MenuItem.vue`；(3) `localNotes.js` 导出 `stripMd`/`resolveLocalTitle`/`isLocalActive`，已被 `FolderTree.vue`(135) 与 `FolderItem.vue`(117) 引用；(4) `EditorArea.vue` 顶栏 文件/编辑/查看 下拉、导出二级子菜单、标签右键菜单共 13+ 处菜单项全部改用 `<MenuItem>`，导出触发项保留 `ref="exportMenuItemRef"`；(5) 重新跑 `npm run build` 验证：直接复用 `.build-verify` 会因 Vite `emptyDir` 调用 safe-delete 删除旧目录触发回收站后端批量删除异常（环境限制，非代码问题），改用全新目录 `.build-verify-fresh` 后 `✓ built in 1.03s` EXIT=0，确认五项任务代码可正常编译。已把 `.build-verify-fresh` 加入 `.gitignore`。
- **实现本地目录自动观察（FileSystemObserver）+ 审计 refreshScan**：(1) `useLocalModeStore.js`：新增 `folderHandleVersion`(ref) 并在 `setFolderHandle`/`clearFolderHandle` 中自增、新增 `getFolderHandle()` 暴露当前目录句柄、把二者加入单例导出（实例已含 `mode/hasFolder/setMode/setFolderHandle/clearFolderHandle/getFolderHandle/folderHandleVersion/getOrCreateDirectory/readFile/writeFile/deleteFile/listDirectoryContents/invalidateCache/addLocalFolder/deleteLocalNote/deleteLocalFolder/renameDirectory`）。(2) `FolderTree.vue`：import 加 `onUnmounted`、localModeStore 解构加 `getFolderHandle/folderHandleVersion/invalidateCache`；新增观察器逻辑——`startWatching()`（特性检测 `'FileSystemObserver' in window`，否则 console.info 静默降级；取 handle 并 `queryPermission({mode:'read'})`/`requestPermission` 兜底，失败则降级；`new window.FileSystemObserver(cb)` + `observe(handle,{recursive:true})`）、`stopWatching()`（`disconnect()` + 清 timer）、`scheduleFsRefresh(records)`（400ms 防抖，合并 create+modify 连发）、`handleFsChanges(records)`（按 `relativePathComponents` 用 `affectedDirOf` 推导受影响父目录，先 `invalidateCache` 失效磁盘缓存再精准刷新：根→`refreshScan()`，已展开子目录→重新 `listDirectoryContents`+`setNodeContents` 避免折叠，未展开子目录仅失效缓存）、`affectedDirOf(components)`；在 `onMounted`+`watch(mode)`+`watch(folderHandleVersion)` 中按"本地且有目录"启停观察，`onUnmounted` 必停。满足需求①②（本地新建本就显式刷新、现再叠加观察兜底；外部磁盘改动由 recursive 观察捕获自动刷新）；手动刷新按钮保留作不支持浏览器兜底。(3) **审计 refreshScan 调用链**（用户要求核验"调用了变量但根本没定义"）：逐项确认 `refreshScan`(hasFolder/scanDirectory/mergeItems/rootItems/loading/scanPromise)、`scanDirectory`(hasFolder/listDirectoryContents)、`mergeItems`(纯参数)、`resetAndScan`(loadedDirs/rootItems/refreshScan)、`setNodeContents`(findNodeByPath/rootItems/loadedDirs)、`findNodeByPath`(纯参数) 全部有定义；新增观察器函数引用的 `invalidateCache/refreshScan/loadedDirs/listDirectoryContents/setNodeContents/getFolderHandle/scheduleFsRefresh/stopWatching` 均存在；全 src grep `localFolderTreeRef`/`onLocalRefresh`/`addLocalNote(`/`folderCache`(组件级) 仅余 store 内合法使用与一行注释，无残留未定义引用。(4) 构建验证 `npm run build -- --outDir .build-verify-fresh4` → `✓ built in 1.23s` EXIT=0（仅 gray-matter eval/chunk 体积无关警告）；清理 `.gitignore` 重复的 `.build-verify-fresh2` 并补 `.build-verify-fresh4`。

- **合并导入文本函数（原优化清单第 6 条「文件导入函数重复」）**：`src/utils/importExport.js` 中 `importMarkdown`/`importFrontmatterMarkdown`/`importTxt` 三兄弟逻辑近乎一致（都 readFile→按扩展名去尾→返回 {title,content,folderId}，仅是否解析 frontmatter 不同），合并为单一 `importTextFile(file, { parseFrontmatter = false, folderId = null } = {})`：非 frontmatter 分支 content 取原始 raw、标题按 /\.(md|markdown|txt)$/i 去尾（顺便修掉原 importMarkdown 只去 .md 漏 .markdown 的小遗漏）；frontmatter 分支用 matter(raw) 解析，title 回退 baseTitle、folderId 回退入参、createTime/updateTime 由 data.created/updated 推断。删除三个旧函数及其导出。同步更新 `EditorArea.vue` 自动导入分支：.txt→importTextFile(file)（纯文本），else(.md/.markdown)→importTextFile(file, { parseFrontmatter: true })；并删除无用的 importMarkdown 导入（grep 确认仅被 import、从未调用）。全 src grep importMarkdown|importFrontmatterMarkdown|importTxt 无残留引用。构建验证 `npm run build -- --outDir .build-verify-fresh5` → `✓ built in 1.11s` EXIT=0（仅 gray-matter eval/chunk 体积无关警告）；已把 .build-verify-fresh5 加入 .gitignore。

- **修复本地模式 5 处 bug（用户实测反馈）**：(1) `FolderTree.vue` 三个右键 handler（startRename/addSubFolder/addNoteInFolder）原 `closeMenu()` 在读取 `menuTarget.value` 之前，导致拿到 null → 子文件夹内新建笔记/文件夹、文件夹重命名全部失效或落到根目录；改为先 `const target = menuTarget.value` 再 `closeMenu()`。(2) `mergeItems` 原"以旧列表为基础、只增不删"→ 磁盘删除/外部改名的条目永久残留；重写为"以 newList 为基础，匹配项保留 contents、未匹配丢弃"，彻底反映删除/改名。(3) `refreshScan` 原未失效磁盘缓存 → 手动刷新读到旧缓存"偶尔失效"；改为开头 `invalidateCache("")` 保证根目录读盘，并支持 `refreshScan(deep=true)` 额外重扫所有已展开子目录（`invalidateCache(dir)`+`listDirectoryContents`+`setNodeContents`）；`Sidebar.handleManualRefresh` 改为 `refreshScan(true)` 深度刷新。(4) `watch(folderHandleVersion)` 原只 `startWatching()` 不扫描 → 首次绑定目录侧边栏空白；改为 `resetAndScan()`+`startWatching()`（清缓存→扫描→启动观察）。(5) 本地笔记三点菜单原缺"重命名"（在线有、本地无，属实现遗漏）；`useLocalModeStore` 新增 `renameLocalFile(dirPath,oldName,newTitle)`（读旧内容→新名写盘→删旧→invalidateCache，沿用 renameDirectory 复制+删旧思路并校验目标已存在避免覆盖），导出并加入 FolderTree 解构；`FolderTree` 的 file 菜单补重命名 MenuItem 及分隔线，新增 `renameLocalNote` handler（取 target→closeMenu→prompt→调 renameLocalFile→同步已打开 note.filename 避免自动保存写回旧名→按 dirPath 刷新对应目录）。构建验证 `npm run build -- --outDir .build-verify-fresh6` → `✓ built in 1.21s` EXIT=0（仅 gray-matter eval/chunk 体积无关警告）；已把 .build-verify-fresh6 加入 .gitignore。

- **修复本地模式删除笔记后标签栏不同步**：根因——本地笔记仅存内存（不写 Dexie），磁盘文件被删（`deleteLocalNote`）后 `noteList`/`openTabs`（标签栏数据源）未清理，已删笔记残留在标签栏。在 `useNoteStore` 新增 `removeLocalNote(filename, dirPath)`（按 filename+dirPath 匹配并移除 noteList/openTabs 记录、必要时切换 activeId，避开 Dexie 以免误删在线数据），导出并加入 FolderTree 解构；两处调用——`confirmDeleteLocalNote` 应用内删除磁盘成功后同步清理（核心场景）、`handleFsChanges` 观察器捕获外部删除 `.md` 文件事件时也调用（覆盖资源管理器直接删除已打开笔记的场景）。构建验证 `npm run build -- --outDir .build-verify-fresh7` → `✓ built in 1.24s` EXIT=0（仅 chunk 体积警告）；已把 .build-verify-fresh7 加入 .gitignore。

- **修复本地重命名笔记后标签栏/侧边栏名字不更新**：根因——标签页(EditorArea.vue:87 显示 note.title)与侧边栏文件项(getNoteTitle→resolveLocalTitle 读 note.title)都渲染 note.title，而 renameLocalNote 只同步了 opened.filename 未同步 opened.title，磁盘改名后内存标题仍是旧值。修复：renameLocalFile 成功后，在 FolderTree.renameLocalNote 中将已打开笔记的 opened.title 一并更新为 newName.replace(/\.md$/i,'')，标签栏与侧边栏随即显示新名；文件名仍同步避免自动保存写回旧名。构建验证 npm run build -- --outDir .build-verify-fresh8 EXIT=0（仅 chunk 体积警告）；已把 .build-verify-fresh8 加入 .gitignore。

- **修复本地重命名后侧边栏名字不更新（嵌套子文件夹 contents 传参 bug）**：标签页已因上一轮同步 note.title 而正确，但侧边栏仍显示旧名。根因——FolderItem.vue 递归渲染子文件夹时（原第60行）传 `:contents="childFolder"`（目录对象本身），而子文件夹内笔记列表的 items 计算属性执行 `(目录对象).filter(...)` 会抛错/停滞，导致二级及以上子文件夹的笔记列表 DOM 卡在重命名前的旧状态，表现为侧边栏名字不更新；根目录/一级子文件夹因根组件传的是 `:contents="item.contents"`（数组）故正常。修复：递归处改为 `:contents="childFolder.contents || []"`。childFolders 计算属性用 `{...i}` 展开会读取 i.contents，setNodeContents 重设 node.contents 后触发重算并把新数组经 contents 传下去，响应式链路通。构建验证 npm run build -- --outDir .build-verify-fresh9 EXIT=0（仅 chunk 体积警告）；已把 .build-verify-fresh9 加入 .gitignore。
