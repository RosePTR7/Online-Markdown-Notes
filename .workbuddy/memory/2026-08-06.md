# 2026-08-06 工作日志

## 保存竞态修复（App.vue）
- 现象：本地/在线模式快速从笔记 A 切到 B（A 仍有未保存编辑）时，A 的未存内容概率被写进 B（串味/互换），偶尔表现为不变。
- 根因：单一共享 `editorContent` 缓冲 + `runSaveLogic`/`flushSave` 读「活的」`currentNote.value.id` 与 `editorContent.value`。`watch(currentNote)` 先 `flushSave()` 再 `editorContent = B内容`；flush 瞬间 currentNote 已是 B 但缓冲区仍是 A 内容 → `updateNote(B.id, A内容)`。概率来自「切走时 A 是否还有待保存内容」（2s 防抖窗口内才触发）。
- 修复（`src/App.vue`）：引入 `pendingSave = { id, content }` 快照，编辑/撤销/重做发生时捕获「当时的笔记 id + 内容」；`applySave`/`flushSave`/`watch(editorContent)` 一律按快照落盘，不再读切换后的活状态。`handleUndo`/`handleRedo` 去掉 `originContent = prevContent`/`isUnsaved=false` 的手动设置，改为显式建立 pendingSave，避免撤销后切走丢失结果。
- `watch(currentNote)` 顺序不变（先 flushSave 再切缓冲），因 flushSave 现在只写快照（A 回 A），天然安全。
- 构建验证 `.build-verify-savesnap` EXIT 0；临时目录用 PowerShell 强删。
- 核心收益：快速连续 A→B→C 切换，每次都把各自笔记的快照写回自身，互不串味；beforeunload/卸载仍走 flushSave 保快照。

## 保存机制二次重构：按 noteId 隔离的「后台预保存」（取代上面的 pendingSave 单一快照方案）
- 用户验证发现 pendingSave 方案仍有「串味」，并提出新方案：标签栏每篇已打开笔记各自独立预保存，切换笔记不打断旧笔记的计时器，旧笔记在后台继续计时并落盘。
- 新建 `src/stores/useSaveManager.js`：纯计时/单飞逻辑，`Map<noteId,{content,timer,saving}>`；`scheduleSave`/`flushNote`/`flushAll`/`removePending`。持久化与落盘后回调由 App 注入（`initSaveManager({persist, afterSave})`），避免与 store 循环依赖。
- 并发结论（已回答用户）：JS 单线程 → 多笔记计时器串行执行，互不并行；隐患只在「全局共享单一 pendingSave/saveTimer」（已消除）；各自写各自文件/记录安全；唯一危险是同一文件两次同时在途写 → 用 `saving` 单飞 + flush 前 clearTimeout 规避。
- `src/stores/useNoteStore.js` 新增 `persistNote(id, content)`：本地笔记重建 frontmatter 后 `writeFile` 落盘（**根因 C：此前本地编辑内容从未写磁盘**）；在线走 `updateNote`。新增 `updateNoteContent(id, content)` 仅内存即时同步（保证 2s 窗口内切回不显示陈旧）。`closeTab` 关闭前 `flushNote`。
- `src/App.vue`：删除旧的 `pendingSave/saveTimer/applySave/flushSave/DEBOUNCE_DELAY`；`editorContent` watch 改调 `scheduleSave` + `updateNoteContent`，内容改回原值则 `removePending`；`watch(currentNote)` 切走**不再 flush**（旧笔记后台继续），仅重置 originContent/isUnsaved；beforeunload/onBeforeUnmount 改 `flushAll`。
- `src/components/MdEditor.vue`：删除 `isBusy` 提前 return（**根因 B：切到站 B 时若 80ms 内刚在 A 敲过字，对 B 的 setValue 被丢弃 → 编辑器仍显示 A 内容，表现为显示串味**），保留 `val===currentVal` 防回环。
- 关键陷阱：`updateNoteContent`/`updateNote`/`persistNote` 必须**原地改属性**，不能 `noteList.value[idx] = {...}` 替换元素——否则 `currentNote` 计算属性引用变化会每键触发「切换笔记」watch（重置 originContent/isUnsaved、关闭查找面板）。
- `EditorArea.applyPolish` 去掉直接 `updateNote` 调用（改走 emit→editorContent watch→scheduleSave→persistNote，避免本地笔记漏写磁盘 frontmatter）。
- 构建：Windows 回收站后端限制导致 vite 清空 dist 时报 `safe-delete` 失败 → 每次构建前用 PowerShell `Remove-Item -Recurse -Force` 清 dist。EXIT 0（仅 js-yaml 外部化 / gray-matter eval / chunk>500kB 警告）。

## UI 定位修复：文件/编辑/查看菜单 + AI 配置悬浮窗
- 现象：点击编辑器上方「文件/编辑/查看」三个按钮，下拉菜单跑到网页左上角 (0,0)；点击「AI配置」出现的悬浮窗在窄屏/缩放下可能超出右边界显示不全。
- 根因：下拉菜单原本靠模板 ref（`fileMenuRef` 等）传按钮元素给 `togglePanel`，模板 ref 自动解包在不同场景有歧义，易取到 null → 坐标回落到默认的 (0,0)。AI 配置 `FloatingPanel` 用固定默认位置 `{x:100,y:80}`，仅显示时钳制一次，未锚定到按钮，窄屏易贴边。
- 修复（`src/components/EditorArea.vue`）：菜单按钮 `@click` 改用 `(e) => togglePanel(panel, e.currentTarget)`，直接用被点击按钮的 DOM 元素取 `getBoundingClientRect`，彻底消除 ref 解包歧义；删除不再使用的 `fileMenuRef/editMenuRef/viewMenuRef`。AI配置按钮改用 `openAiConfig()` 按按钮坐标算 `aiConfigPos` 并传给 `FloatingPanel` 的 `:default-position`。
- 修复（`src/components/FloatingPanel.vue`）：`watch(visible)` 在显示时先 `position = {...defaultPosition}` 重新锚定，再 `clampToViewport()`，保证 AI 配置面板每次都在按钮下方且钳制在视口内。
- 构建：PowerShell 强删 dist 后 `npm run build` EXIT 0（仅既有警告）。

## AI 配置悬浮窗输入框右侧贴边（box-sizing 根因）
- 现象：AI 配置面板右侧输入框紧贴面板右边框，左侧却有 16px 间距（不对称）。
- 根因：项目默认 `box-sizing: content-box`（同文件 inputPanel 输入框之前单独加了 `box-border` 印证）。`FloatingPanel` 内容区虽有 `px-4`（左右各16px），但 AI 配置输入框用 `w-full + border + p-2` 且**未加 `box-border`** → `w-full` 只算内容宽度，输入框自身边框+内边距加在外面，整体向右溢出盖住右侧 16px 内边距 → 右侧视觉贴边、左侧留空，不对称。
- 修复（`src/components/EditorArea.vue`）：给 AI 配置三个输入框、AI 润色 textarea、查找/替换框补 `box-border`，使 `w-full/flex-1` 把边框与内边距计入宽度，左右间距对称。

## 新增：从浏览器外拖文件到侧边栏导入（拖拽导入）
- 需求：不用系统文件选择器，直接把外部文件拖进侧边栏即导入；不支持的格式弹「不支持该格式的文件，仅支持 ...」提示，外观复用重命名同款 Modal（useModalStore.confirm）。
- 重构（`src/stores/useNoteStore.js`）：把导入编排从 EditorArea 抽到 store 统一入口 `importFiles(fileList)`，菜单文件选择器与拖拽共用，消除重复。新增 `SUPPORTED_IMPORT_EXT = ['md','markdown','txt','json']`；内部 `addNoteFromData`/`importJSONData`(含备份确认弹窗)/`importSingleFile`/`importFiles`。`importFiles` 区分支持/不支持：支持的逐个导入，不支持的收集后弹**一次** confirm 提示（样式同重命名弹窗），不中断已支持的导入；全支持则 toast「导入成功」。
- `EditorArea.vue`：删除 `handleFileSelectAuto`/`addNoteFromImport`/`handleJSONImport` 及无用 import（`importTextFile/importJSON/noteTable/folderTable`、`addNote/importNote` 解构），`doImport` 的 input.onchange 改为 `(e)=>noteStore.importFiles(e.target.files)`。
- `Sidebar.vue`：根容器加 `@dragenter/@dragover/@dragleave/@drop`；用 `dragCounter` 计数器（enter+1/leave-1）避免拖到子元素时高亮闪烁；`onDragEnter` 仅当 `dataTransfer.types` 含 `Files` 才计数（忽略网页内部拖拽）；`onDrop` 调 `noteStore.importFiles(files)`。新增 `isDragOver` 高亮遮罩（dashed 边框 + import 图标 + 提示文字），`pointer-events-none` 不挡 drop。
- `Icon.vue`：新增 `import` 图标（上传箭头入托盘）。
- 已知限制（与现有菜单导入一致）：本地模式下拖入导入走在线笔记存储（addNote→Dexie），不直接写磁盘文件；本地模式侧边栏树不显示，但会出现在搜索结果。如需本地盘导入可后续扩展。
- 构建：PowerShell 强删 dist 后 `npm run build` EXIT 0（仅既有警告）。

## 新增：回收站（软删除）+ ZIP 整库导出
- 需求（用户确认）：数据安全两类——① 回收站（删除可恢复）；③ 整库导出为 ZIP。②「本地导入落盘」不必做，继续放 IndexedDB。知识管理暂不做。
- 范围决策：**回收站 + ZIP 都只针对「在线笔记/文件夹」（IndexedDB）**；**本地笔记/文件夹删除保持永久**（与「本地放 IndexedDB」一致，且不碰磁盘）。
- `src/utils/db.js`：新增 `version(3)`，`notes`/`folders` 表各加 `deletedAt` 索引（0/undefined=未删，>0=删除时间戳）。老库打开自动升级，undefined 不进索引、不影响查询。
- `useNoteStore.js`：`delNote` 在线笔记改为软删除（`noteTable.update(id,{deletedAt:Date.now()})` + 移出 noteList/openTabs/activeId）；本地笔记仍是纯内存移除（不碰 Dexie/磁盘）。新增 `getDeletedNotes`/`restoreNote`(置 deletedAt=0 并推回 noteList)/`purgeNote`(真删)/`emptyRecycleBin`(删全部已软删)/`removeNotesFromListByFolder`(供删文件夹时清笔记与标签)。`loadNotes` 过滤 `deletedAt`，重启后回收站内容不回列表。
- `useFolderStore.js`：`deleteFolder` 改为软删除（自身+子文件夹+子笔记置 deletedAt，移出 folderList/noteList），不再真删；新增 `getDeletedFolders`/`restoreFolder`(级联恢复子树)/`purgeFolder`(真删文件夹+其笔记)/`emptyRecycleBinFolders`。`loadFolders` 过滤 deletedAt。`deleteFolder` 调用点在 FolderTree:539（仅在线分支，本地走 `deleteLocalFolder` 永久）。注：useFolderStore 反向 import useNoteStore 无循环（useNoteStore 不 import useFolderStore）。
- `src/components/RecycleBin.vue`（新建）：Teleport 覆盖层，复用 Modal 同款卡片风格 + isDark；列出已删笔记/文件夹（含删除时间），逐个「恢复/彻底删除」，顶部「清空回收站」；彻底删除/清空前用 `useModalStore().confirm({confirmDanger:true})` 二次确认。
- `Sidebar.vue`：操作按钮区新增「回收站」按钮（trash 图标）→ `showRecycleBin` 开关挂载 `<RecycleBin>`。
- `Icon.vue`：新增 `trash`、`close` 图标（24x24 描边）。
- `src/utils/zip.js`（新建）：无第三方依赖的极简 ZIP 写入器（STORE 法、UTF-8 文件名 bit 0x0800、CRC32、本地头+中央目录+EOCD），导出 Blob。
- `importExport.js`：新增 `exportAllAsZip()`（仅在线+未软删的笔记/文件夹；笔记按 folderId 链生成目录层级、带 Frontmatter 导出为 `<标题>.md`，文件名冲突自动加 `(n)` 去重；额外含 `backup.json` 完整数据便于精确恢复）+ `downloadBlob()`。
- `EditorArea.vue`：导出子菜单加「导出全部为 ZIP」→ `exportAllZip`（`downloadBlob`，文件名 `notes-backup-YYYY-MM-DD.zip`）。
- `FolderTree.vue`：在线删除笔记/文件夹的确认文案由「此操作无法撤销」改为「删除后可到『回收站』恢复」（本地删除仍保留「无法撤销」）。
- 构建：PowerShell 强删 dist 后 `npm run build` EXIT 0（仅既有警告）。

## 新增：编辑器图片插入（Vditor upload handler + data URL 内嵌）
- 用户确认做「编辑器图片插入」增强（此前担心 Vditor 成品不支持，实为未接线）。
- 关键契约（读 vditor 源码确认）：`upload.handler(files)` 的**返回值只用作错误提示**（字符串被当 tip 显示），内容必须由 handler 自行 `insertValue` 插入；且 `upload.multiple` 必须显式 `true`，否则 `uploadFiles` 只传 1 个文件给 handler（限制多图粘贴/拖入）。`insertValue(value, render=true)` 是公开方法（dist/types/index.d.ts:61）；默认 IR 工具栏含「上传」按钮（代码块内才禁用）。
- 方案：图片以 **data URL 内嵌 Markdown**——在线模式随笔记存 IndexedDB，本地模式随 .md 落盘，重载后均可直接渲染，且无需附件表 / URL 解析（预览即原生 img）。比「附件表 + 自定义 URL 解析」更稳更省。
- `src/utils/imageEmbed.js`（新建）：`fileToMarkdown(file)` 单文件→`![名称](dataURL)`。位图超 1600px 最长边智能缩放（PNG/WebP 保留透明→输出 PNG；其余填充白底→JPEG 0.85 更省体积）；SVG/GIF 原样 base64（保留矢量/动画）。解析失败退回原始 base64。
- `src/components/MdEditor.vue`：Vditor 初始化加 `upload: { accept:'image/*', multiple:true, handler }`。handler 过滤 `image/*`（非图片返回提示字符串），`Promise.all(fileToMarkdown)` 后 `vditor.insertValue(parts.join('\n')+'\n')`，再主动 `emit('update:modelValue', vditor.getValue())` 触发上层自动保存（insertValue 不一定触发 input 回调）；失败返回提示字符串。支持粘贴 / 拖入 / 点工具栏「上传」三种入口。
- 本地模式无需额外改动：data URL 随 persistNote 写进 .md 文件（自包含、可移植）。
- 构建：PowerShell 强删 dist 后 `npm run build` EXIT 0（仅既有警告）。

## 修复：Dexie "Schema was extended" 警告（db.js 升版）
- 现象：控制台持续打印 `Schema was extended without increasing the number passed to db.version(). Dexie will add missing parts and increment native version number to workaround this.`
- 根因：回收站功能（`deletedAt`）是**就地**加在 `version(3)` 的（仅未提交工作区），而用户浏览器库此前已按已提交的 v2（或 Dexie 补偿态）创建；如今 `version(3)` 声明的 schema 比已持久化内容「多」，Dexie 每次打开都重新检测并补偿，持续告警。git 历史证实 `HEAD`（ca7c106）只有 v1/v2，`deletedAt` 从未进过提交。
- 修复（`src/utils/db.js`）：保留 v1/v2 历史原样，新增 `version(4)`（schema 与 v3 完全一致）作为全新的、未被补偿的版本；v1→v4 连续声明保证迁移链不断（Dexie 要求 1..max 全声明）。注释强调「以后扩展 schema 一律新增 version(5)/version(6)…，严禁就地改旧版本号」。
- 构建：PowerShell 强删 dist 后 `npm run build` EXIT 0（仅既有警告）。

## 本地模式隐藏「回收站」按钮（Sidebar.vue）
- 背景：回收站只针对在线（IndexedDB）软删除，本地删除是永久的，本地模式下打开回收站只会显示在线残留项（通常为空），属误导。
- 改动（`src/components/Sidebar.vue`）：给「回收站」按钮加 `v-if="localModeStore.mode.value === 'online'"`，本地模式下不再渲染该按钮。其余按钮（新建笔记/新建文件夹）不受影响。
- `<RecycleBin>` 仍始终挂载但默认 `visible=false`，因按钮不可达，本地模式不会打开，无副作用。
- 构建：PowerShell 强删 dist 后 `npm run build` EXIT 0（仅既有警告）。
